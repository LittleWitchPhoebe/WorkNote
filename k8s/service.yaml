apiVersion: v1
kind: Service
metadata:
  name: todo-list
  labels:
    app: todo-list
spec:
  type: ClusterIP
  selector:
    app: todo-list
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
