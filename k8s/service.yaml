apiVersion: v1
kind: Service
metadata:
  name: worknote
  namespace: worknote
  labels:
    app: worknote
spec:
  type: NodePort
  selector:
    app: worknote
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
